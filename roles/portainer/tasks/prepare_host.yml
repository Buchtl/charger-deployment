---
- name: Create /srv/portainer/data
  become: true
  file:
    path: /srv/portainer/data
    state: directory
    owner: "{{ charger_user }}"
    group: "{{ charger_group | default(charger_user) }}"
    mode: '0755'
